sigmoid function (sig(x)) is the following:
    sig(x)=1/(1+e(^-x))
derivative of the sigmoid function is:
    sig'(x) = sig(x) * (1 - sig(x))

if
    sig(x)=0.2,
it means that
    sig'(x)= 0.2 * (1 - 0.2) = 0.2 * 0.8 = 0.16